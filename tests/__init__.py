# security-guard: ignore-file
"""Test package for Security Guard.

Run all tests with::

    python -m unittest discover -s tests -v

Tests use only the standard library (``unittest``, ``unittest.mock``) so the
suite has zero extra dependencies and runs anywhere ``security_guard.py`` runs.
"""
